{
  programs.readline = {
    enable = true;

    variables = {
      editing-mode = "vi";
      completion-ignore-case = "on";
      enable-bracketed-paste = "on";
      show-mode-in-prompt = "on";
    };
  };
}
